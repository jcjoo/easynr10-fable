# continue.md — Estado da sessão (atualizado em 04/07/2026)

Handoff para a próxima sessão do **EasyNR10 v2** — reescrita completa do sistema legado em `../easyNR10`.

## O que é este repositório

Monorepo Bun da reescrita (v2) do EasyNR10, plataforma de conformidade NR-10 da PSO Engenharia.
O legado (NestJS + React, em `../easyNR10`) é a **referência de funcionalidade** — leia-o antes de
reimplementar qualquer módulo (instrução explícita do usuário: *"não podemos perder funcionalidades"*).

## Documentos-chave

| Documento | Conteúdo |
|---|---|
| `projeto.md` | Requisitos (RF/RNF), modelo de domínio, **dicionário de dados**, diagramas Mermaid, roteiro F0–F6 |
| Guia de design (artifact) | https://claude.ai/code/artifact/a8681cde-e957-4975-89c3-88f3aa12ff0f — tokens, tipografia, componentes, dark mode (v0.3) |
| `README.md` | Comandos de dev e docker |

## Stack (decidida e implementada)

- **Monorepo Bun** (workspaces): `apps/web`, `apps/api`, `packages/shared` (Zod/enums), `packages/db` (Drizzle+migrations+seeds). **Node NÃO é necessário** (validado em 04/07/2026): as imagens oven/bun trazem um wrapper `node`→bun (por isso vite/tsc buildam no container sem Node real) e o mesmo fallback existe em máquinas só-bun; os scripts de vite e drizzle-kit usam `bunx --bun` para não caírem no Node do host quando ele existe no PATH (shebang node dos CLIs). Types também só-bun: nenhum workspace declara `@types/node` — api e db têm só `@types/bun` e `"types": ["bun"]` no tsconfig (o `@types/node` continua existindo TRANSITIVAMENTE porque o próprio bun-types depende dele; é assim por design, e é o que tipa `node:path` do vite.config do web).
- **API**: **Hono** (fetch-native no `Bun.serve`; era Fastify até 04/07/2026 — decisão do usuário, os shims Request↔Fastify e a dupla serialização de JSON sumiram; Elysia descartado por competir com o tRPC via Eden) + tRPC v11 + better-auth (email/senha; Google por env) + Drizzle + Postgres 17. Roda com **bun** (dev e container). `@types/bun` + `"types": ["bun"]` no tsconfig da api.
- **Web**: React 19 + Vite + TanStack Router (URL = fonte da verdade) + TanStack Query + Tailwind 4 + zustand mínimo (contexto ativo, tema). Fontes self-hosted (Fontsource): Archivo Variable, IBM Plex Sans/Mono.
- **Infra**: MinIO (presigned URLs), Gotenberg (PDF, ainda não usado), Mailpit; nginx serve o SPA e proxya `/api`.
- Tipos ponta a ponta: web importa `AppRouter` da api; schemas Zod compartilhados.

## Como rodar

```bash
# Stack completa (testada): http://localhost:8081 (WEB_PORT no .env)
docker compose up -d --build

# Dev local: postgres na porta 5433 (5432 é do legado!)
docker compose -f docker-compose.dev.yml up -d
bun install && bun run db:migrate && bun run db:seed
bun run dev:api   # :3000
bun run dev:web   # :5173 (proxy /api)
```

**Config central no `.env`** (03/07/2026): portas do host (WEB_PORT=8081, MINIO_PORT=9100,
MINIO_CONSOLE_PORT=9101, DEV_*) e credenciais são variáveis `${VAR:-default}` nos dois
compose; endpoints internos (minio:9000, database:5432) ficam no compose. Stack e infra de
dev compartilham MINIO_PORT — não subir as duas juntas.

**Login de teste** (ambos ambientes): `admin@pso.dev` / `senha-forte-dev-123` (role admin via UPDATE no banco — não há tela de cadastro/gestão de usuários ainda; ver README).

**Dados de teste na stack**: em 03/07/2026 (noite) o usuário excluiu manualmente as empresas seed (Metalúrgica Aurora, Têxtil, Empresa Teste — soft delete, dados recuperáveis no banco) e criou **Empresa A / Unidade A**, onde estão os testes mais recentes (EPIs importados por planilha com CA vinculado). Em 04/07/2026 foram inseridos **via SQL direto** (para validar o F4): 130 diagnósticos em 3 ondas (75/40/10 dias atrás, aderência evoluindo 25%→54%), 53 ações (a maioria com prazo vencido) e 9 documentos no PIE com validades variadas — são dados simulados, não criados pela UI.

## Estado da implementação (roteiro do projeto.md)

### Feito e validado (Playwright na stack + screenshots)
- **F0 Fundação**: auth (better-auth, cookie), `unitProcedure` (isolamento de tenant), layout com sidebar contextual (fluxo do client-test: Empresas → [empresa] Painel/Unidades → [unidade] Painel/PIE/Avaliação(Diagnóstico, Plano)/Relatórios/Grupos de Registro), header com breadcrumb (empresa→/empresas, unidade→/unidades) + busca global Ctrl+K + **switch dark mode** (persistido, tokens `.dark`).
- **F1**: CRUD de empresas e unidades na UI (admin, incluindo excluir com confirmação no card ⋯). **Painel de usuários** (`/usuarios`, admin, `pages/usuarios.tsx` + router `users`): lista usuários e libera/revoga acesso por unidade (checkbox de empresa marca todas as unidades; estado indeterminado quando parcial; grant reativa membership soft-deletada via upsert na PK). Usuário de teste: `cliente@pso.dev` / `senha-cliente-123` (role client, acesso a Guarulhos+Sorocaba). Falta: criar usuário pela UI (sign-up só via API).
- **F2 PIE completo**: lista estilo drive (pastas em cima, mesma tabela), pasta atual na URL (`?pasta=`), upload via **presigned URL** com modal fiel ao legado (dropzone, **select de documento padrão** — 30 nomes seedados, complemento quando nome tem `- *` com preview, checkbox sem referência, vencimento/aviso), versionamento (nova versão só pelo histórico, restaurar reutiliza storage_key), editar/excluir doc, renomear/excluir pasta (cliente: só vazia; admin: cascata com subpastas+documentos, com dialog de confirmação), context menu (clique direito) nos documentos, ações rápidas no hover da linha (baixar/editar/renomear antes do ⋯), visão "apenas documentos" (`?ver=documentos`, toggle no breadcrumb; `documents.listBySubtree` lista tudo abaixo da pasta atual com coluna Local clicável), filtro de vencimento portado do legado (`?venc=&de=&ate=`, presets A vencer/Vencidos/Personalizado em `pie/expiry-filter.tsx` — usuário pediu para tirar os de 15/30/60 dias —, filtragem client-side, chip com ✕ para limpar), árvore de pastas na sidebar (`layout/folder-tree.tsx`, ancestrais da pasta ativa auto-expandem; recolhível pelo chevron no item PIE, persistido em `easynr10.pie-tree`), busca global inclui pastas e documentos da unidade ativa, **gerador de estruturas** (esquemas POR UNIDADE copiados dos modelos globais no 1º uso; CRUD com editor de árvore; "Gerar em [pasta atual]" = exec do legado).
- **F3 núcleo**: 90 normas NR-10 + 101 requisitos seedados do legado; itens de adequação por unidade (gerar; ordenação natural de códigos), diagnóstico com **escala de aderência de 5 níveis** definida pelo usuário em 03/07/2026: inexistente/inadequada/parcial/suficiente/plena (cores bad/alert/warn/suf/ok — token `alert` laranja criado; "sem avaliação" = ausência de registro; fora de escopo = is_active; migration 0004 mapeou insuficiente→inadequada e conforme→plena). **Aderência geral agregada** no topo do Diagnóstico: média dos scores (0/25/50/75/100%) ponderada pelo peso da norma, só itens ativos avaliados; faixas 0-20/21-40/41-70/71-90/91-100 dão rótulo+emoji+frase (`adherenceBands` em shared/enums). Filtro por chips persistido na URL **com contadores** + filtros Sem avaliação/Com avaliação, histórico, **plano de ação auto-gerado** (aderência < Plena com prazo) com status pendente/em andamento/concluída/cancelada + "prazo vencido".
- **F3 motor de evidências (§7.6, 03/07/2026)**: página de config do item (`/diagnosticos/$adequacyItemId`, `pages/diagnostico-item.tsx`, ícone ⚙ no hover da linha) com status ativo, **orientação da unidade** (coluna nova, migration 0003) e CRUD de requisitos (documento/parecer/grupo; grupo exige grupo de cadastro + doc padrão — FK `default_document_id` corrigido para o catálogo, como no legado). Requisitos copiados do catálogo `norm_requirement` no generate E lazy no 1º acesso (`ensureItemRequirements`, conta excluídos p/ não ressuscitar). Dialog de avaliação tem seção Evidências (doc do PIE via `listBySubtree`, parecer textual, grupo com `expandGroupRequirement` + sugestão por pasta do item/termo do doc padrão); `diagnose` grava `evidence`+`evidence_item` (snapshot); histórico expande evidências (`diagnosticEvidences`). `registers.listGroups` mínimo criado (o módulo completo de grupos continua pendente — sem grupos, o tipo group fica sem opções).

- **F4 Relatórios e dashboards (04/07/2026)**: router `reports` na API (`routers/reports.ts`) com builders compartilhados — `adequacySnapshot` (itens ativos + último diagnóstico), `nonConformityRows`, `documentSituationRows` (caminho da pasta + situação vencido/a_vencer/em_dia/sem_validade via `warn_days_before`, default 30), `actionPlanRows` (escopo pendencias|todas, flag `overdue`), `unitOverview` (aderência ponderada + distribuição + grupos documentais + contagens de ações/documentos), `timelineSeries` (evolução da aderência por varredura de diagnósticos — port do timeline do legado adaptado à escala v2: só itens já avaliados na data entram na média ponderada) e `companyOverview` (aderência por unidade visível). **Exportação (RF22)**: rota HTTP `GET /api/reports/export?unitId&type&format&scope` (`report-export.ts`, registrada no main.ts) com cookie de sessão + checagem de membership igual ao unitProcedure; CSV com BOM UTF-8 + `;` + CRLF (Excel pt-BR); **PDF via Gotenberg** (HTML A4 paisagem com cabeçalho da marca → `/forms/chromium/convert/html`; `GOTENBERG_URL` no env, default localhost:3010 = DEV_GOTENBERG_PORT). **Web**: `pages/relatorios.tsx` (3 relatórios em chips na URL `?tipo=&escopo=`, tabelas com pills, botões CSV/PDF como `<a download>`); **Painel da unidade** (`unit-home.tsx`) = dashboard RF19: hero de aderência geral (banda+frase), faixa de distribuição por status (gaps 2px + legenda clicável → diagnósticos filtrados), medidores por grupo documental (cor pela banda), **gráfico SVG próprio de evolução** (`components/charts/adherence-timeline.tsx`: linha 2px + wash 10%, crosshair + tooltip por pointer/teclado, período na URL `?periodo=30d|90d|12m` → daily/weekly/monthly), tiles de plano de ação e documentos; **Painel da empresa** (`company-panel.tsx`) = cards por unidade com % e medidor. Labels novos no shared: `actionStatusLabels`, `documentSituations/Labels`, `timelineIntervals`. Validado na stack com Playwright (screenshots + download real de CSV/PDF). Sem mudança de schema (nenhuma migration). **Ordenação + filtros (04/07/2026, pedido do usuário)**: todas as colunas das 3 tabelas são ordenáveis (clique no th, seta + aria-sort; nulos sempre no fim; norma com `compareNormCodes`, movido do adequacy.ts para o **shared** junto com `normalizeText`); filtros por relatório — busca textual sem acento, select de grupo documental e chips de status com contador (plano: Pendências default/Prazo vencido/Concluídas/Canceladas/Todas — substituíram o checkbox `escopo`); tudo na URL (`?status=&grupo=&q=&ord=&dir=`) e o **export aplica os mesmos filtros** (`applyFilters` em report-export.ts, mesmos params na query string). Depois o usuário pediu **todas as tabelas do sistema ordenáveis**: mecanismo extraído para `ui/sortable.tsx` (`SortableTh`/`PlainTh`/`sortRows`/`toggleSort`/`sortSearch` — este último é o validateSearch reutilizável; `unitSection` no router agora aplica `sortSearch` por padrão) e aplicado em Diagnóstico, Plano de Ação, PIE (pastas só reordenam pelo Nome, ficam no topo; navegação preserva ord/dir), Cadastros (colunas dinâmicas = `campo:<key>`; kind=document ordena pelo nome do doc vinculado) e Usuários.

- **Prioridade no plano de ação + peso fora do front (04/07/2026, pedido do usuário)**: o `importance_weight` da norma **não aparece mais em lugar nenhum do front** (colunas Peso removidas do Diagnóstico, do relatório de Não Conformidades e dos exports CSV/PDF; `nonConformityRows` nem envia o campo ao cliente) — ele só alimenta as médias ponderadas e a **prioridade** do plano de ação. Fórmula em `actionPriority` (shared/enums), fiel ao legado (`normalizacao.ts`): `riscoBruto = (nota máx − nota) × peso`, amplitude 16 (peso máx 4 × nota máx 4); o percentual das faixas é o lado score (100 − risco%) e as faixas definidas pelo usuário são ❌ Alta 0–50% · ⚠️ Média 51–90% · ✅ Baixa 91–100%. Calculada **no servidor** (`adequacy.actionItems` e `actionPlanRows` devolvem `priority`, sem o peso); pill colorida ordenável na página Plano de Ação, no relatório Plano de Ação e nos exports. Nota: interpretei o percentual como complemento do risco (não `nota×peso/16` literal — senão peso baixo viraria sempre Alta); confirmar com o usuário se algum caso parecer estranho.
- **Diagnóstico (04/07/2026, pedido do usuário)**: coluna Prazo removida da tabela (o prazo segue no dialog/histórico); no lugar entrou **Última avaliação** (data do último diagnóstico; texto "Sem avaliação" quando nunca avaliado). Na coluna Aderência, item sem avaliação exibe a pill **Inexistente** (antes era "Sem avaliação") — os chips de filtro Sem/Com avaliação continuam. Hero do painel da unidade: `items-baseline` → `items-center` + `leading-none` no número (o % parecia desalinhado). O `Dialog` (ui) ganhou prop `size` (`md` default = max-w-lg; `lg` = max-w-3xl) — o dialog de avaliação usa `lg` (estava apertado em telas grandes) com Responsável + Ação recomendada lado a lado. **Filtros de status componíveis (04/07/2026, pedido do usuário)**: chips de status alternam e COMPÕEM (união dos recortes) no Diagnóstico (`StatusFilter` agora recebe `value: DiagnosticFilter[]`) e nos 3 relatórios; na URL vira CSV (`?status=inexistente,inadequada`, validado token a token no router); no plano de ação "Pendências" (null) limpa e "Todas" é exclusivo; o export espelha (`applyFilters` divide o status por vírgula, união). **Código nos campos kind=document (04/07/2026, pedido do usuário)**: além do documento vinculado, o campo tem um código cadastrado (ex.: nº do CA do EPI) guardado em `metadata[field.key]` — sem mudança de schema/API. Em registros.tsx `allFields` substituiu `textFields` (todos os campos têm valor texto): input no editor (com hint), código + chip na mesma célula da tabela, importável por planilha e ordenação pelo código (fallback: nome do doc vinculado). **Não rodar `bunx prettier` neste repo** — não há config e o default (aspas duplas, width 80) reformata tudo; seguir o estilo manual (aspas simples). **Preview de documentos no PIE (04/07/2026, pedido do usuário)**: `presignPreview` no s3.ts (GetObject com `inline` + `ResponseContentType` do version.mimeType) + `documents.previewUrl` (retorna url+mimeType); no web, clicar no **nome do documento** abre dialog `size="xl"` (novo tamanho no Dialog, max-w-5xl) com iframe (PDF/text/*), `<img>` (image/*) ou fallback "indisponível"+Baixar; também no menu ⋯/contexto ("Visualizar") e por versão no histórico. Validado na stack (imagem renderiza; PDF confirmado por headers 200/inline/application-pdf — iframe de PDF não renderiza em screenshot no Chromium headless). Docs de teste "Teste Preview PDF/Imagem" ficaram na pasta Colaboradores da Unidade A. **Padronização de componentes (04/07/2026, pedido do usuário — "filtro de relatórios diferente do de diagnósticos")**: primitivos únicos em ui/ — `pill.tsx` (Pill base: ponto + rótulo), `status-pill.tsx` agora concentra TODAS as pills semânticas + mapas de cor (`StatusPill`, `SituationPill`, `ActionStatusPill` c/ overdue, `PriorityPill`) e exporta `adherenceDots`/`situationDots` p/ chips; `filter-chips.tsx` (`FilterChips`: chip padrão do Diagnóstico com ponto+contador — semântica do clique fica no chamador); `tabs.tsx` (`SegmentedTabs`, o tablist dos Cadastros). Aplicado em: relatorios (seletor de relatório virou SegmentedTabs; chips de status ganharam pontos e o MESMO tamanho do Diagnóstico; pills locais removidas), plano-de-acao e usuarios (pills locais → ui), pie (ExpiryPill usa Pill base), unit-home (período 30d/90d/12m virou SegmentedTabs), registros (tablist → SegmentedTabs), status-filter (wrapper fino de FilterChips). Regra: **filtro = FilterChips, troca de visão = SegmentedTabs, status readonly = pills de status-pill.tsx** — não criar chip/pill inline nas páginas. **Logo → placeholder (04/07/2026)**: o usuário reprovou a logo atual; `layout/logo-placeholder.tsx` mostra o espaço com borda tracejada e as medidas recomendadas — sidebar **150×28** (slot útil 216×48) e login **200×36**, arte final @2x (300×56 / 400×72), PNG ou SVG transparente, versões clara+escura. **Revisado em 04/07/2026 (pedido do usuário)**: placeholder REMOVIDO — a logo real de `assets/` voltou, maior: sidebar `h-9` com header `h-16` (era h-5/h-12) e login `h-10`; variantes clara/escura via `dark:hidden`/`dark:block`. Validado nos dois temas. **Alerta de aderência geral removido da página Diagnóstico** (04/07/2026, pedido do usuário) — o hero com banda/frase fica SÓ no Painel da unidade; o cálculo ponderado local do diagnosticos.tsx foi apagado junto.

- **Seletor de documento com navegação de pastas + botão "Novo" (04/07/2026, pedido do usuário)**: `components/pie/document-picker.tsx` (`DocumentPickerDialog`) substitui TODOS os `<select>` de documento — breadcrumb, pastas clicáveis, busca na subárvore atual, check no selecionado; `startPath` (nomes desde a raiz) abre já na pasta do grupo: avaliação usa o `documentGroup` da norma, Vincular CA usa `registerBasePath[target].slice(0,-1)` (ex.: Equipamentos/EPI). Sidebar ganhou botão **Novo** (estilo drive, só com unidade ativa): Criar pasta/Adicionar documentos (→ PIE `?nova=pasta|documento`; documento espera o usuário entrar numa pasta — o flag sobrevive ao goTo — e abre o upload), Criar diagnóstico (navega), Novo colaborador/equipamento (→ `?novo=1`, RegisterPage abre o editor). Rotas: pie valida `nova`, `unitSection` valida `novo`.

- **Revisão SOLID / arquivos sobrecarregados (04/07/2026, pedido do usuário)**: API ganhou **camada de serviço** — `services/reports.ts` concentra os builders de relatório (router tRPC e `report-export.ts` consomem dali; sumiu a dependência export→router); `routers/adequacy.ts` (552 linhas) virou **diretório** `routers/adequacy/{shared,items,requirements,diagnostics,actions,index}.ts` (módulos exportam objetos de procedures, index compõe `router({...spread})` — namespace `adequacy.*` inalterado p/ o cliente). Web: `lib/format.ts` (formatDate/formatDateTime/formatBytes pt-BR — eram 6 cópias); extraídos `pie/document-versions-dialog.tsx`, `pie/document-preview-dialog.tsx`, `diagnostico/assessment-dialog.tsx` (montado com `key={target.id}` p/ resetar estado) e `registros/import-dialog.tsx`. Tamanhos: pie 1073→930, registros 940→805, diagnosticos 690→272. Validado na stack (avaliação+histórico, versões+preview, export CSV, dashboard). Convenção de dependência: páginas→componentes→ui; routers→services→db; regra pura no shared.
- **projeto.md reescrito (04/07/2026, pedido do usuário)**: agora é o documento do **EasyNR10 original** (v1.0), sem enquadramento de refatoração/migração — estrutura profissional: visão/problema/atores, escopo, RF (com status ✅) e RNF, arquitetura (stack Hono/Bun, containers, organização do código, navegação), modelo de domínio + dicionário de dados ATUALIZADO (escala 5 níveis, register_target, custom_field, register_document_link), **regras de negócio** (§6: scores/faixas de aderência, fórmula da prioridade, versionamento, situação documental, motor de evidências), fluxos (sequência), segurança/tenancy, infra, qualidade, roadmap F0–F6 e riscos. O usuário deletou a ANALISE-EASYNR10.md do working tree (coerente com o reenquadramento; segue no histórico do git).

- **"Decorators" de permissão + matriz (04/07/2026, pedido do usuário)**: no tRPC (funcional) o equivalente a decorators são os **procedure builders por permissão** — trpc.ts agora tipa `Meta { permission }` (`initTRPC.meta<Meta>()`) e cada builder carimba a sua: `publicProcedure`=publica, `protectedProcedure`=autenticado, `adminProcedure`=admin, `unitProcedure`=membro-da-unidade. **`bun run permissions`** (`apps/api/src/permissions.ts`) enumera `appRouter._def.procedures` (mapa plano caminho→procedure, meta em `_def.meta`) e gera **`PERMISSOES.md`** na raiz (72 procedures + 3 rotas HTTP; falha com exit 1 se alguma procedure ficar sem meta). A visualização já pegou um furo real: `companies.byId` era só `autenticado` e vazava nome de empresa por UUID — corrigido (cliente precisa de membership em alguma unidade da empresa; validado 403 na stack com cliente@pso.dev). Regra: endpoint novo SEMPRE nasce de um builder de permissão; rodar `bun run permissions` após mexer em routers.

- **Páginas 403/404 (04/07/2026, pedido do usuário)**: `pages/error-pages.tsx` (ErrorShell comum: código gigante mono + título + frase + Voltar/Ir para o início; `NotFoundPage`, `ForbiddenPage`, `RouteErrorPage` genérica com "Tentar de novo"). Router: `defaultNotFoundComponent`/`defaultErrorComponent` + **guarda de UUID** `requireUuidParams` em todas as 8 rotas paramétricas (`beforeLoad` inline `({ params }) => requireUuidParams(params)` — anotar o handler direto QUEBRA a inferência de params do TanStack; sem a guarda, `/qualquer-coisa` casava com `/$companyId`). QueryClient: `throwOnError: isForbiddenError` (+ retry pulando FORBIDDEN) faz o 403 do tRPC estourar no boundary. Validado: rota inexistente → 404 no layout; cliente sem vínculo abrindo unidade alheia → 403 tela cheia.

- **Fluxos do "Novo" em modal + context menus no PIE (04/07/2026, pedido do usuário)**: `layout/new-menu.tsx` substituiu o NewButton da sidebar — TODOS os itens em modal: Adicionar documentos e Criar pasta usam `pie/folder-picker.tsx` (`FolderPickerDialog`: navegação por pastas + "Destino: X" + confirmar; `allowRoot` p/ pasta) e Criar diagnóstico usa `diagnostico/item-picker.tsx` (busca nos itens ativos → abre `AssessmentDialog`). O flag `?nova=` do PIE foi REMOVIDO (rota, effect, goTo) — só `?novo=1` dos cadastros continua. No PIE: botão "Nova pasta" removido do header; **context menu nas pastas** (Abrir/Renomear/Nova pasta dentro/Excluir) e **na área da lista** (Nova pasta; + Enviar documento quando há pasta ativa) — linhas de doc/pasta fazem stopPropagation no contextmenu; o **input de nova pasta virou linha inline da tabela após a última pasta** (Esc cancela, botão Cancelar). Validado tudo na stack. Nota: UNIQUE de folder (unit,parent,name) NÃO pega duplicata na raiz (parent_id NULL — NULL≠NULL no Postgres); testes criaram 3 "Pasta Teste Novo" duplicadas (soft-deletadas) — considerar índice parcial p/ raiz no futuro.

- **Modal de equipamento com tipo trocável + estrutura padrão por grupo (04/07/2026, pedido do usuário)**: o editor de equipamento tem select **Tipo de equipamento** (troca entre os 4; campos default+personalizados e o caminho da pasta seguem o tipo SELECIONADO no modal, não a aba — query `editorCustomFields` própria); trocar o tipo re-seleciona a estrutura padrão do novo grupo. **`register_target_setting`** (migration 0008): estrutura de pastas padrão por grupo-alvo, configurada no dialog **"Configurar grupo"** (ex-"Campos personalizados", agora com o select "Estrutura de pastas padrão do grupo" + os campos) — vem **pré-selecionada** (opcional, dá para trocar/remover) ao criar colaborador/equipamento. API: `registers.targetSettings`/`setTargetSetting` (matriz regenerada, 74 procedures). Nota: trocar o tipo ao EDITAR mantém a pasta existente no PIE (caminho é histórico).

- **Papéis com mapeamento de permissões + criação de usuários (04/07/2026, pedido do usuário)**: `member_role` (manager/viewer, nunca aplicado) **morreu** — migrations 0009/0010 criam **`app_role`** (name, is_system, permissions jsonb) com Gestor/Leitor seedados (backfill manager→Gestor, viewer→Leitor) e trocam `membership.role` por `role_id NOT NULL`. Catálogo de ações no shared: `unitActions` = pie.manage/diagnostico.manage/plano.manage/cadastros.manage (leitura = ser membro; admin ignora papéis). `unitProcedure` agora carrega `ctx.unitPermissions` (join membership→app_role) e **`unitAction('<ação>')`** guarda as ~30 mutations (varridas); meta ganhou `action` → PERMISSOES.md tem coluna "Ação do papel" (79 procedures). **users router**: `create` (better-auth signUpEmail no servidor + papel global — dialog "Novo usuário" na tela Usuários), `roles/createRole/updateRole/removeRole` (sistema imutável; bloqueia excluir papel em uso — dialog "Papéis e permissões" com checkboxes que salvam na hora), `grant` exige `roleId` (AccessDialog: select "Papel para novas liberações" + select de papel por unidade liberada). **`services/visibility.ts`** (`visibleUnits`/`canAccessCompany`) centralizou o branch admin/cliente de companies.list/byId, units.listByCompany e reports.companyOverview. Dialogs em `components/usuarios/`. Validado na stack: papel "Técnico" criado, usuário `leitor@pso.dev`/`senha-leitor-123` criado pela UI com papel **Leitor** na Unidade A — lê tudo (200), diagnose/criar pasta = 403 "Seu papel nesta unidade não permite esta ação". Pegadinha: `drizzle-kit generate` com add+drop de coluna na mesma tabela pede prompt interativo (sem TTY trava) — gerar em DUAS migrations (aditiva com backfill manual no SQL, depois a de drop). **Clareza dos dois níveis (04/07/2026, pedido do usuário)**: tabela de Usuários tem colunas **"Papel global"** (pill Admin | **Usuário** — rótulo trocado de "Cliente") e **"Papel nas unidades"** (agregado dos vínculos vindo do `users.list`: pills "Gestor ×2" etc.; admin = "Acesso total"; sem vínculo = "Sem acessos"; ignora unidades soft-deletadas).

- **Tela própria de Papéis por empresa + catálogo 1:1 (04/07/2026, pedido do usuário)**: `app_role.company_id` (migration 0011; UK virou (company_id,name)) — `NULL` = padrão do sistema (Gestor/Leitor, imutáveis, valem para TODA empresa automaticamente = "padrões ao criar empresa"); preenchido = papel custom da empresa. Página **`/$companyId/papeis`** (`pages/papeis.tsx`, link "Papéis" na seção da empresa da sidebar, admin): lista à esquerda (Padrão pill / contador n/4) + detalhe com **mapeamento 1:1** — cada ação mostra os ENDPOINTS reais que destrava via **`users.permissionCatalog`** (enumera o appRouter em runtime — import lazy `./index` p/ evitar ciclo) + seção "Leitura" com os endpoints de membro; Renomear/Duplicar (padrão → cópia editável da empresa)/Excluir (bloqueado se em uso). `users.roles` agora exige `companyId` (globais + customs da empresa, com contagem de uso); `createRole` exige companyId; `updateRole` aceita name; `grant` valida papel×empresa da unidade. AccessDialog refeito: papéis POR EMPRESA (componente `CompanyAccess` com query própria; marcar libera como Gestor, select por unidade). RolesDialog do /usuarios foi REMOVIDO (papéis agora só na página da empresa). Papel "Técnico" ficou global (company_id NULL, aparece em todas — mover/excluir pela UI se incomodar); "Gestor Local" é cópia de teste na Empresa A. Matriz: 80 procedures.

- **Permissões granulares com descrição (04/07/2026, pedido do usuário)**: o catálogo virou **`unitActionCatalog`** no shared — 18 capacidades em 4 grupos, cada uma com `action`/`group`/`label`/`description` (ex.: `pie.documento.enviar`, `diagnostico.avaliar`, `cadastros.vinculos`); `unitActions`/`UnitAction` derivados dele. As ~30 mutations foram re-varridas para as ações granulares (mapa no continue: pasta criar/renomear/excluir; documento enviar/editar/excluir/restaurar; estruturas; diagnóstico avaliar/configurar/requisitos/gerar; plano.status; cadastros itens/importar/vinculos/campos/config). **Migration 0012** (custom, `drizzle-kit generate --custom`): expande as ações antigas `*.manage` dos papéis existentes para os itens equivalentes (validado: Gestor 18, Técnico 4). Página de Papéis reorganizada: seções por grupo com checkbox "marcar todos" (indeterminado parcial) + cada item com checkbox individual, código, **descrição** e chips dos endpoints; contador n/18 na lista. PERMISSOES.md agora mostra a ação granular por endpoint.

- **Leitura por módulo editável + módulos somem da sidebar (04/07/2026, pedido do usuário)**: catálogo foi a **24 itens em 6 grupos** — cada módulo ganhou `*.ler` (pie/diagnostico/plano/cadastros/painel/relatorios) com descrição "Desligado, o módulo some da navegação"; as ~30 queries de unidade viraram `unitAction('*.ler')` (units.byId segue livre p/ o shell) e o **export HTTP** checa `relatorios.ler` no papel. **Migration 0013** (custom): adiciona as 6 leituras a TODOS os papéis ativos (comportamento preservado; Leitor=6, Gestor=24). Web: **`units.myPermissions`** + hook `lib/use-unit-permissions.ts` (staleTime 5min; `can`=estrito, `canShow`=otimista p/ evitar flicker) — sidebar esconde NavItems sem leitura (PIE inclui a árvore; SubLabels condicionais), NewMenu filtra itens por permissão de escrita, e as **queries de layout são gateadas** (folder-tree/global-search/pickers/assessment com `enabled: loaded && can('pie.ler')`) — SEM o gate, um FORBIDDEN de módulo bloqueado derruba a página inteira via throwOnError (foi exatamente o bug: a árvore da sidebar montava otimista e disparava folders.list). 403 por link é automático (query principal da página toma FORBIDDEN → boundary). Validado com papel "Só Diagnóstico" (leitor@pso.dev está com ele na Unidade A): sidebar só com Diagnóstico; PIE/Relatórios por link → 403; export → 403. Matriz: 81 procedures.

- **Painel de usuários POR EMPRESA (04/07/2026, pedido do usuário)**: página **`/$companyId/usuarios`** (`pages/usuarios-empresa.tsx`; link "Usuários" na seção da empresa, entre Unidades e Papéis, admin) — lista só quem tem membership em unidade da empresa (novo `users.listByCompany`: agrega papéis por usuário NAQUELA empresa). **`users.createForCompany`** cria o usuário (better-auth, papel global sempre client) JÁ vinculado às unidades marcadas com um papel da empresa (valida papel×empresa e unidades×empresa) — dialog `components/usuarios/company-user-create-dialog.tsx` (nome/email/senha + select de papel + checkboxes das unidades, mínimo 1). `AccessDialog` ganhou prop opcional **`companyId`** (escopa a árvore a uma empresa e invalida `listByCompany`) — reusado pelo painel. Validado: leitor listado, cliente (sem vínculo) fora; "Gestora Empresa A" (gestora@empresa-a.dev/senha-gestora-123) criada pela tela com Gestor na Unidade A; dialog de acessos sem Empresa B. Matriz: 83 procedures.

- **Entrada guiada por permissão + atalhos de contexto único (05/07/2026, pedido do usuário)**: acabou o 403 ao clicar numa unidade sem `painel.ler` — `beforeLoad` de `/$companyId/$unitId` busca `units.myPermissions` (via `queryClient.ensureQueryData`, cache compartilhado com o hook da sidebar) e, sem painel, **redireciona para a primeira seção legível** na ordem da sidebar (`unitEntrySections` no router: pie→diagnosticos→plano-de-acao→relatorios→equipamentos; nenhuma legível = segue pro painel e o 403 fica como resposta honesta; não-membro = FORBIDDEN do ensureQueryData estoura no errorComponent → 403). **Cliente com UMA empresa** entra direto nela: `redirectSingleCompany` no `beforeLoad` de `/` e `/empresas` (companies.list length===1 → `/$companyId/unidades`) e o item "Empresas" some da sidebar; **cliente com UMA unidade** na empresa pula a lista: `beforeLoad` de `/$companyId/unidades` redireciona para a unidade (que resolve a seção pela permissão) e "Unidades" some da sidebar. Admins ficam FORA dos atalhos (precisam das listas para criar) — gate por `session.user.role !== 'admin'`; o `hasSession` do router virou `getSession()` (cacheia a sessão inteira, não só boolean). Sidebar: queries companies.list/units.listByCompany só `enabled` para não-admin, esconder só quando `isSuccess` (sem flicker). Validado na stack (Playwright): leitor@pso.dev (papel "Só Diagnóstico", 1 empresa/1 unidade) loga e cai DIRETO em /diagnosticos sem Empresas/Unidades na sidebar e /empresas devolve pra lá; gestora@empresa-a.dev (Gestor) cai no painel da unidade; admin mantém /empresas e a sidebar completa.

- **Botões de escrita somem sem permissão (05/07/2026, pedido do usuário — "carlos vê criar pasta/enviar documento sem poder")**: as PÁGINAS agora usam `useUnitPermissions` (o gate existia só na sidebar/NewMenu). **PIE** (`pie.tsx`): Estruturas (`pie.estruturas.gerenciar`, inclui o botão do estado vazio), Enviar documento (`pie.documento.enviar`, header + menu de contexto da seção), Nova pasta (`pie.pasta.criar`, menus de contexto), renomear/excluir pasta (lápis + ⋯ somem sem ambas), Editar/Excluir do documento (lápis + itens do ⋯/contexto), textos de estado vazio sem sugerir ação proibida; contextmenu da seção nem abre sem nada permitido. `DocumentVersionsDialog` ganhou props `canUpload`/`canRestore` (Enviar nova versão / Restaurar). **Diagnóstico** (`diagnosticos.tsx`): Gerar itens (`diagnostico.gerar`), engrenagem de configurar (`diagnostico.configurar||requisitos`); `AssessmentDialog` vira SOMENTE LEITURA sem `diagnostico.avaliar` (descrição+histórico ficam, formulário some). **Item** (`diagnostico-item.tsx`): Salvar/checkbox/orientação por `configurar` (inputs `disabled`), requisitos (form novo, lixeiras, Remover todos) por `requisitos`. **Plano** (`plano-de-acao.tsx`): ⋯ de status por `plano.status`. **Cadastros** (`registros.tsx`): Importar (`cadastros.importar`), Configurar grupo (`campos||config`), Vincular em massa e chips por linha (`vinculos` — chip vira texto sem ✕), Novo/editar/excluir (`itens`). **NewMenu**: papel sem NENHUMA escrita esconde o botão "Novo" inteiro (menu ficaria vazio; early-return DEPOIS dos hooks). Regra: `can()` estrito (falso até carregar) — botão de escrita não pisca para quem não tem. Validado na stack (Playwright) com usuário **cobaia.teste@pso.dev / senha-cobaia-teste-123** (criado pela UI admin com o papel **Cobaia** = pie.ler+diagnostico.ler, mesmo papel do carlos@pso.dev): zero botões de escrita no PIE/Diagnóstico, dialog de avaliação só com histórico, sem botão Novo; gestora (Gestor) segue vendo tudo.

- **TypeScript 6 + ignoreDeprecations (05/07/2026, pedido do usuário — "ignore deceptions 6")**: `"ignoreDeprecations": "6.0"` no `tsconfig.base.json` para silenciar os avisos de deprecação que o TS 6 do editor emite (opções que somem no TS 7). PEGADINHA: o TS 5.9 REJEITA o valor "6.0" (TS5103) — por isso o repo subiu para **typescript ^6.0.3** nos 5 package.json. Única quebra do TS 6: side-effect import não resolvido (TS2882) — `import '@fontsource-variable/archivo'` virou `.../index.css` (o `vite/client` tipa `*.css`). Typecheck + build limpos.

- **Navegação interna vira filhos na sidebar (05/07/2026, pedido do usuário)**: os SegmentedTabs de ESCOLHA DE CONTEÚDO sumiram das páginas — **Relatórios** virou grupo na sidebar (SubLabel + 3 filhos: Não Conformidades ⚠ / Situação Documental 🕐 / Plano de Ação ☑, cada um `search={{ tipo }}`) e os 4 tipos de equipamento viraram grupo **Equipamentos** (Elétricos ⚡ / Ferramentas 🔧 / EPI ⛑ / EPC 🚧) sob Cadastros (Colaboradores ficou direto sob Cadastros). `NavItem` ganhou prop `search` (o TanStack Link acende por subset do search — `?tipo=` na URL). O tipo de equipamento saiu do useState para a **URL** (`?tipo=eletrico|ferramenta|epi|epc`, validado no router com default eletrico; relatórios ganhou default `nao-conformidades` no validateSearch para o filho acender mesmo sem ?tipo). `registros.tsx`: h1 mostra o tipo ativo (registerTargetLabels), sort e ?novo=1 preservam o tipo na navegação. SegmentedTabs continua nos usos NÃO-navegacionais (período do gráfico, modo de visão do PIE). Validado na stack (Playwright/gestora): filhos navegam e acendem individualmente, sort mantém `?tipo=`.

### Próximos passos (ordem combinada com o usuário)
1. ~~Grupos de Registro~~ **feito em 03/07/2026, com mudança de modelo**: o usuário removeu o módulo genérico — `register_group`/`register_item` dropados (migration 0005). `employee`/`equipment` são standalone (unitId, name, folderId no PIE, metadata) + `custom_field` (campos personalizados por unidade/módulo; defaults do sistema em `defaultRegisterFields` no shared: Função/Matrícula e Fabricante/Identificação). Telas em `pages/registros.tsx` (RegisterPage genérico; Equipamentos com **abas por tipo** — cada grupo-alvo tem colunas default próprias em `defaultRegisterFields` e campos custom POR ALVO, migration 0006). **Estrutura de pastas FIXA** (migration 0007 dropou register_folder): `Colaboradores/Lista de Colaboradores/[nome]` e `Equipamentos/<Tipo>/Lista de <Tipo>/[nome]` (`registerBasePath` no shared); o **esqueleto completo é criado automaticamente** (`ensureRegisterSkeleton`: na criação da unidade e lazy nas listagens de cadastros); ao criar item a pasta nasce automática, opcionalmente com **estrutura de pastas** dentro (`ensureFolderStructure` exportado de folder-schemas). **Campos kind=document** (CA do EPI): vinculados a documentos do PIE via `register_document_link` (1 doc → N itens; chip na tabela com tom de vencimento + ✕; botão "Vincular CA" faz vínculo em massa com checkboxes) — base para automações de vencimento (diagnóstico/alertas, ainda não implementadas). **Importação por planilha** (.xlsx/.csv via SheetJS no cliente, `codepage: 65001` para CSV UTF-8): de-para de colunas com auto-match por nome, upsert por nome (`importEmployees`/`importEquipment`). Requisito tipo group usa **alvo fixo** `target_group` (colaboradores|eletrico|ferramenta|epi|epc); `expandGroupRequirement` expande employees ou equipment por tipo; `evidence_item` tem `employee_id`/`equipment_id`. Sidebar: seção "Cadastros".
2. ~~Motor de evidências~~ **feito em 03/07/2026** (ver F3 acima) — falta só a tabela `referenced_document` (RF11).
3. ~~Relatórios e dashboards (F4)~~ **feito em 04/07/2026** (ver acima). Fora do escopo entregue: filtros extras nos relatórios (por norma/grupo/período, o legado tinha DTOs ricos), relatório "documento" por pasta (pathPrefix) e o painel externo por iframe (módulo Painel do legado — avaliar se ainda é desejado).
4. Notificações (F5, pg-boss não instalado ainda): e-mails de vencimento de documento e prazo de ação; in-app.
5. `referenced_document` (RF11) + automações de vencimento dos campos kind=document (CA de EPI → alerta/diagnóstico).

## Pegadinhas descobertas (não repetir)

- **`apply` é nome reservado** em router tRPC (usar `applyToUnit`).
- **Bun não hoisteia tudo**: Dockerfile da api copia `/app` inteiro do estágio deps (node_modules dos workspaces).
- **drizzle-kit tem shebang node** → não roda na imagem bun; migrations via script programático `packages/db/src/migrate.ts` (o CMD do container roda migrate no boot).
- Install com `--frozen-lockfile` exige os package.json de TODOS os workspaces na imagem.
- **nginx**: upstream com resolver dinâmico (`resolver 127.0.0.11` + variável), senão morre se a api não subiu.
- **S3**: assinar presigned com `S3_PUBLIC_ENDPOINT` (browser) ≠ `S3_ENDPOINT` (rede interna); MinIO publica :9100 (console :9101) — 9000/9001 do host são de outro projeto (site-minio).
- Porta **5432 ocupada pelo legado** → dev usa 5433.
- Menus dropdown em tabelas com overflow: renderizar em **portal** com posição fixa (`ui/row-menu.tsx` tem `Menu` reutilizável para ⋯ e context menu).
- Layout: shell `h-screen overflow-hidden`, só o `<main>` rola (senão a sidebar some com o scroll do body). Estilo "Drive" (pedido do usuário): sidebar/header no fundo `paper`, `<main>` é um painel `surface` arredondado; tabelas ficam direto no painel (sem `Card` — o componente Card só sobrou no login).
- Ordenar códigos de norma **naturalmente** (10.2 < 10.11) — `compareNormCodes` em `adequacy.ts`.
- Enum `document_group` real do legado: `instalacoes | instrucoes_e_procedimentos | colaboradores | equipamentos` (já corrigido em código+docs).
- **Exclusão em cascata** (03/07/2026, pedido do usuário): `units.remove`/`companies.remove` fazem soft delete de TODA a árvore (`apps/api/src/cascade.ts`: pastas, documentos, itens de adequação, diagnósticos/evidências, ações, cadastros, vínculos, estruturas, memberships) e **purge no MinIO** (`purgeUnitObjects` em s3.ts — apaga todas as versões + delete markers do prefixo `units/<id>/`, via endpoint interno). Exclusão de documento individual segue soft delete sem tocar no storage (registro legal). Unidades órfãs das exclusões antigas do usuário foram limpas via API em 03/07.
- **ensureUnitSchemas** (folder-schemas): o "já copiou os modelos?" tem que contar estruturas EXCLUÍDAS também — senão excluir a última estrutura ressuscita os modelos globais no próximo list (bug corrigido em 03/07/2026).
- Seeds portados do legado em `packages/db/src/seeds/`: `default-documents.json` (30), `norms.json` (90), `default-folder-schema.json` (sem a raiz duplicada "Colaboradores(2)").

## Preferências do usuário (importante)

- **Sempre olhar o sistema legado antes de reimplementar** — fidelidade de funcionalidade primeiro, melhorias depois. Não inventar colunas/valores ("a necessidade vai aparecer depois, deixa apenas o essencial").
- Validar visualmente: rodar na stack Docker + **screenshots via Playwright** (`playwright-core` instalado no scratchpad, chromium do sistema em `/usr/bin/chromium`) e enviar com SendUserFile.
- UI em pt-BR seguindo o guia de design (tokens em `apps/web/src/styles.css`); status de aderência com as cores confirmadas; âmbar só como marca.
- Assets da marca vieram do legado: logo `fullLogo(.Dark).png`, favicon = ícone das 3 barras, ícones de arquivo/pasta em `ui/icons.tsx`.
- Manter `projeto.md` (dicionário/diagramas) atualizado quando o modelo mudar.
- Repositório tem git init mas **nenhum commit ainda** (usuário não pediu).

## Mapa rápido de arquivos

- API routers: `apps/api/src/routers/` — companies, units, folders, folder-schemas, documents, default-documents, adequacy (itens+diagnóstico+plano), registers, reports (overview/timeline/relatórios). Export CSV/PDF: `apps/api/src/report-export.ts`.
- Tenant/auth: `apps/api/src/trpc.ts` (unitProcedure), `auth.ts` (better-auth).
- Web páginas: `apps/web/src/pages/` — pie.tsx (grande), diagnosticos.tsx, plano-de-acao.tsx, relatorios.tsx, unit-home.tsx (dashboard), company-panel.tsx, registros.tsx; section-placeholder.tsx ficou sem uso (mantido p/ fases futuras). Gráfico: `components/charts/adherence-timeline.tsx`.
- Componentes PIE: `apps/web/src/components/pie/` (upload-document-dialog, folder-schemas-dialog).
- UI primitivos: `apps/web/src/components/ui/` (button, field, select, card, dialog, row-menu/Menu, status-pill, status-filter, icons — inclui FileTypeIcon por MIME/extensão, crumbs, page — container padrão full-width de toda página).
- Schema DB: `packages/db/src/schema/` (4 migrations aplicadas; enum de aderência = insuficiente/parcial/suficiente/conforme).
